MFQ Project

Compilation
  cd src/
  make clean install

Running
  ./src/simulation -f input -o output
  Input and output are relative file paths.

  Ex. From proj. root dir. a.k.a (the current dir.) 
  ./src/simulation -f doc/sample_mlfq_input -o doc/sample_mlfq_output
